apps:
    - name: maprules
      script: index.js
      env_testing:
          NODE_ENV: testing
          PORT: 3000
          HOST: 'localhost'
      env_development:
          NODE_ENV: development
          PORT: 3000
          HOST: 'localhost'
      env_production:
          NODE_ENV: production
          PORT: 3000
          HOST: 'localhost'
